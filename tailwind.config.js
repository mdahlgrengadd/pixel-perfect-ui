module.exports = {
  content: ['./src/**/*.{ts,tsx,js,jsx}', './public/**/*.html'],
  theme: {
    extend: {
      spacing: {
        'dock-w': 'var(--spacing-dock-w, 280px)',
        'tools-w': 'var(--spacing-tools-w, 66px)',
        'menu-h': 'var(--spacing-menu-h, 24px)',
        'options-h': 'var(--spacing-options-h, 36px)',
        'tab-h': 'var(--spacing-tab-h, 18px)',
        'panel-header': 'var(--spacing-panel-header, 18px)',
      },
      minWidth: {
        'app': 'var(--min-width-app, 980px)',
      },
      width: {
        'dock-w': 'var(--spacing-dock-w, 280px)',
        'tools-w': 'var(--spacing-tools-w, 66px)',
      },
      height: {
        'menu': 'var(--spacing-menu-h, 24px)',
        'options': 'var(--spacing-options-h, 36px)',
        'tab': 'var(--spacing-tab-h, 18px)',
      },
      colors: {
        'background': 'hsl(var(--background))',
        'foreground': 'hsl(var(--foreground))',
        'panel': 'hsl(var(--panel))',
        'panel-foreground': 'hsl(var(--panel-foreground))',
        'panel-header': 'hsl(var(--panel-header))',
        'border': 'hsl(var(--border))',
        'border-light': 'hsl(var(--border-light))',
        'border-dark': 'hsl(var(--border-dark))',
        'input': 'hsl(var(--input))',
        'muted': 'hsl(var(--muted))',
        'muted-foreground': 'hsl(var(--muted-foreground))',
        'accent': 'hsl(var(--accent))',
        'accent-foreground': 'hsl(var(--accent-foreground))',
        'workspace': 'hsl(var(--workspace))',
        'workspace-foreground': 'hsl(var(--workspace-foreground))',
        'document': 'hsl(var(--document))',
        'document-foreground': 'hsl(var(--document-foreground))',
        'document-shadow': 'hsl(var(--document-shadow))',
        'ruler': 'hsl(var(--ruler))',
        'ruler-foreground': 'hsl(var(--ruler-foreground))',
        'ruler-tick': 'hsl(var(--ruler-tick))',
        'ruler-cursor': 'hsl(var(--ruler-cursor))',
        'highlight': 'hsl(var(--highlight))',
        'shadow': 'hsl(var(--shadow))',
        'tool-active': 'hsl(var(--tool-active))',
        'tool-hover': 'hsl(var(--tool-hover))',
        'layer-selected': 'hsl(var(--layer-selected))',
        'layer-selected-foreground': 'hsl(var(--layer-selected-foreground))',
        'menu-hover': 'hsl(var(--menu-hover))',
        'menu-hover-foreground': 'hsl(var(--menu-hover-foreground))',
        'tab-bar': 'hsl(var(--tab-bar))',
        'tab-active': 'hsl(var(--tab-active))',
        'tab-inactive': 'hsl(var(--tab-inactive))',
        'swatch-background': 'hsl(var(--swatch-background))',
        'swatch-foreground': 'hsl(var(--swatch-foreground))',
        'checker': '#888888',
      },
    },
  },
  plugins: [],
};
